apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sm-service
  namespace: sm-platform
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - sm-service.sm-platform.example.com
      secretName: sm-service-tls
  rules:
    - host: sm-service.sm-platform.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sm-service
                port:
                  number: 8300
