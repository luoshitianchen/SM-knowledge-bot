apiVersion: v1
kind: Service
metadata:
  name: {{ include "sm-service.fullname" . }}
  labels:
    {{- include "sm-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sm-service.selectorLabels" . | nindent 4 }}
