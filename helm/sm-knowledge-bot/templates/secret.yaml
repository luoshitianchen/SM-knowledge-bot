apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sm-service.fullname" . }}-secret
  labels:
    {{- include "sm-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secret }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
