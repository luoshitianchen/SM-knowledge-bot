apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sm-service.fullname" . }}-config
  labels:
    {{- include "sm-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
